CREATE TABLE "Post" (
    "id" bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "private" boolean,
    "text" varchar,
    "createdDate" timestamp,
    "deleted" boolean,
    "id_user" bigint,
    "id_group" bigint
);