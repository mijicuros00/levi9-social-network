CREATE TABLE "Event" (
     "id" bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
     "id_location" bigint,
     "startDate" timestamp,
     "endDate" timestamp,
     "id_user" bigint,
     "id_group" bigint
);